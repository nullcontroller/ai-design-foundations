---
import { getCollection } from "astro:content";
import Site from "../layouts/Site.astro";
import ContentIndex from "../components/ContentIndex.astro";
import RelatedPublications from "../components/RelatedPublications.astro";
import SectionVisual from "../components/SectionVisual.astro";
import { publicEntry, url } from "../lib/site";
import PageHeader from "../components/PageHeader.astro";
import SectionHeading from "../components/SectionHeading.astro";
import ThemeArticleIndex from "../components/ThemeArticleIndex.astro";
const all = await getCollection("pages", publicEntry);
const practices = all
  .filter((e) => e.data.layer === "practice")
  .sort((a, b) => (a.data.order ?? 999) - (b.data.order ?? 999));
const themeLinks = practices.map((entry) => ({
  title: entry.data.title,
  href: url(entry.id),
}));
---

<Site title="実践知" section="practices" themeLinks={themeLinks}>
  <PageHeader icon="practices" title="実践知" />
  <SectionVisual
    name="lifecycle-roadmap"
    alt="段階的な適用と継続的な改善を表すシンボル"
  />
  <p class="lead">
    設計原則を、AI導入・教育・横展開・開発Workflowへ適用する。
  </p>
  <ThemeArticleIndex links={themeLinks} />
  <SectionHeading icon="practices" title="業務・開発プロセスへの組込み" />
  <ContentIndex entries={practices} />
  <RelatedPublications
    summary="設計判断の背景や、個別の問題意識を扱った公開記事です。"
    entries={all.filter(
      (e) =>
        e.data.section === "practices" &&
        e.data.source?.original_type === "article",
    )}
  />
</Site>
