---
interface Props {
  links: { title: string; href: string }[];
}

const { links } = Astro.props;
---

<details class="theme-toc-mobile">
  <summary>このテーマの記事</summary>
  <ol>
    {links.map((item) => (
      <li><a href={item.href}>{item.title}</a></li>
    ))}
  </ol>
</details>
